import React from "react";

interface Props {}

/**
 *
 * some logic here to handle login functionality
 * import login card here
 * @param props
 */
const LoginForm = (props: Props) => {
  return <div></div>;
};

export default LoginForm;
